{
  "name": "anonymous-chat-backend",
  "version": "1.0.0",
  "description": "Privacy-first anonymous chat backend — no database, no logs, no persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "audit:runtime": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "8.2.1",
    "cors": "2.8.5",
    "dotenv": "^17.4.2",
    "express": "5.2.1",
    "ipaddr.js": "^2.2.0",
    "proxy-addr": "^2.0.7",
    "redis": "6.1.0",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "socket.io-client": "^4.8.3"
  }
}
